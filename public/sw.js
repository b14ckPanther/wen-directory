// This is a very basic service worker.
// It doesn't do much, but it's enough to make your app installable.

self.addEventListener('fetch', (event) => {
  // This event listener is required to make the app installable.
  // We're not doing any complex caching here yet.
});